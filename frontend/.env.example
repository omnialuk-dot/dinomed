# Frontend env (Vite)
# In produzione (Vercel) imposta questo valore nelle Environment Variables:
#   VITE_API_BASE=https://<tuo-backend>.onrender.com
#
# In locale puoi usare .env.local (vedi sotto).
VITE_API_BASE=
